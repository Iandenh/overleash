# --- Overleash Configuration Example ---
#
# Copy this file to .env and fill in the required values.
# Variables that are commented out (#) are optional or show alternative configurations.

# ---------------------------------
# Core Configuration (Required)
# ---------------------------------

# The upstream URL of your Unleash instance (without the /api path)
export OVERLEASH_UPSTREAM=https://unleash.your-company.com

# Comma-separated client token(s) from your Unleash instance
export OVERLEASH_TOKEN=*:development.your-secret-token


# ---------------------------------
# Common Optional Features
# ---------------------------------

# Set the reload frequency for polling the upstream. Examples: "5m" for 5 minutes, "1h" for 1 hour.
# Use "0" to disable polling (recommended if using delta streaming or webhooks).
export OVERLEASH_RELOAD=1m

# Enable verbose logging for debugging purposes
# export OVERLEASH_VERBOSE=true

# Use the upstream's delta streaming endpoint for near-instant updates instead of polling
# export OVERLEASH_DELTA=true

# Enable the webhook endpoint to allow Unleash to trigger a refresh
# export OVERLEASH_WEBHOOK=true


# ---------------------------------
# Storage Configuration
# ---------------------------------

# The default storage backend is "file".
# Use "redis" for multi-instance (HA) setups where overrides need to be synchronized.
export OVERLEASH_STORAGE=file
# export OVERLEASH_STORAGE=redis

# --- Redis Configuration (only needed if OVERLEASH_STORAGE=redis) ---
#
# # Address of your Redis server
# export OVERLEASH_REDIS_ADDRESS=localhost:6379
# # Password for your Redis server (if any)
# export OVERLEASH_REDIS_PASSWORD=
# # Redis database number to use
# export OVERLEASH_REDIS_DB=0
#
# # --- Redis Sentinel for High Availability ---
# # Uncomment the following lines if you are using Redis Sentinel instead of a standalone Redis
# # export OVERLEASH_REDIS_SENTINEL=true
# # The name of the master node in your Sentinel configuration
# # export OVERLEASH_REDIS_MASTER=mymaster
# # Comma-separated list of your Sentinel server addresses
# # export OVERLEASH_REDIS_SENTINELS=sentinel1:26379,sentinel2:26379


# ---------------------------------
# Prometheus Metrics
# ---------------------------------

# Uncomment to enable the Prometheus metrics endpoint for monitoring
# export OVERLEASH_PROMETHEUS_METRICS=true
# export OVERLEASH_PROMETHEUS_METRICS_PORT=9100


# ---------------------------------
# Docker Compose Specific
# ---------------------------------
# These variables are used by the docker-compose.yml file, often for Traefik proxy configuration.

# The hostname you want to use for Overleash (e.g., for a Traefik rule)
export HOST_NAME=overleash.test

# The name of the Docker network your proxy (e.g., Traefik) is connected to
export NETWORK_NAME=proxy


# ---------------------------------
# General Container Settings
# ---------------------------------

# Set the timezone for the container to ensure correct log timestamps
export TZ=Europe/Amsterdam